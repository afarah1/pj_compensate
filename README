* About

Compensate Akypuera's intrusion in pj_dump traces

* Building

`make`

* Usage

`./pj_compensate --help`

* Configuration

`vi Makefile`

* Dependencies

- gsl

If you don't have pkg-config, adapt the Makefile accordingly (should be `-lgsl
-lgslcblas -lm`).
